// This is a combined test file that imports our dialog tests
// We can run just this file to test our dialog components

// Import the dialog component tests
import './dialog-example/dialog-example.component.spec';
import './dialog-test/dialog-test.component.spec';

describe('Dialog Tests', () => {
  it('should run all dialog tests', () => {
    // This is just a placeholder test to ensure the file is loaded
    expect(true).toBeTruthy();
  });
}); 